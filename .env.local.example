# MediFlow AI — copy this to `.env.local` before running the app.
#
#   cp .env.local.example .env.local
#
# ---------------------------------------------------------------------------
# Supabase. Both of these are PUBLIC values — they are compiled into the
# browser bundle and any visitor can read them. They are safe to share.
# What protects the data is row-level security in the database, not secrecy
# of this key.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://padokkwfeoxsnwitwkty.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_03k8KEReQpRd3Z0SGqpYGQ_HNn_2z2W

# ---------------------------------------------------------------------------
# n8n webhook for the "Ask MediFlow" assistant.
#
# DELIBERATELY LEFT BLANK. This one IS a secret — anyone holding the URL can
# trigger the workflow. It is read only on the server (never NEXT_PUBLIC_) and
# is not shipped in this archive.
#
# Ask Adam for the value, or copy it from the Vercel project settings.
# The app runs fine without it; only the Ask MediFlow chat is unavailable.
# ---------------------------------------------------------------------------
N8N_CHAT_WEBHOOK_URL=
